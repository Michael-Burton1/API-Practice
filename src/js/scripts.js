let inputAge = 1
let mercuryOutput = 0
export function mercuryAge(){
  const mercuryMultiplyer = .25
  mercuryOutput = (inputAge / mercuryMultiplyer);
  // console.log(mercuryOutput);
  return mercuryOutput;
};

export function venusAge(){
 
};